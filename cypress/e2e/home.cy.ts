import { createOnboardedUser } from "./utils";

describe("Sign Up & Connect Instagram", () => {
  it("should allow a user to sign up and connect Instagram", () => {
    createOnboardedUser();
  });
});
